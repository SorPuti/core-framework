# Variáveis de ambiente - Multi-tenant. Docs: 02-settings.md, 32-tenancy.md. Troubleshooting: 99-faq-troubleshooting.md
# Edite DATABASE_URL se seu PostgreSQL usar outro user/senha. Depois: core migrate && core run

APP_NAME={{project_name}}
ENVIRONMENT=development
DEBUG=true
SECRET_KEY=change-me-in-production

# PostgreSQL (obrigatório para multi-tenant)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost/{{project_name}}

# Tenancy (opcional; padrões já definidos em src/settings.py)
TENANCY_HEADER=X-Tenant-ID
TENANCY_FIELD=workspace_id
