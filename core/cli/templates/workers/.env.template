# Variáveis de ambiente - Workers. Docs: 02-settings.md, 31-workers.md. Troubleshooting: 99-faq-troubleshooting.md
# Edite DATABASE_URL e REDIS_URL se necessário. Depois: core migrate && core runworker

APP_NAME={{project_name}}
ENVIRONMENT=development
DEBUG=true
SECRET_KEY=change-me-in-production

# PostgreSQL
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost/{{project_name}}

# Redis (fila de tasks)
REDIS_URL=redis://localhost:6379/0

# Tasks (opcional; padrões em src/settings.py)
TASK_DEFAULT_RETRY=3
TASK_DEFAULT_RETRY_DELAY=60
TASK_WORKER_CONCURRENCY=4
