# Variáveis de ambiente - Kafka. Docs: 02-settings.md, 30-messaging.md. Troubleshooting: 99-faq-troubleshooting.md
# Ajuste DATABASE_URL, KAFKA e REDIS conforme seu ambiente. Depois: core migrate && core run

APP_NAME={{project_name}}
ENVIRONMENT=development
DEBUG=true
SECRET_KEY=change-me-in-production

# PostgreSQL
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost/{{project_name}}

# Kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Redis
REDIS_URL=redis://localhost:6379/0
