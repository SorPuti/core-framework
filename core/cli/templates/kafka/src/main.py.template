"""
App entry point - Event-driven architecture.
Docs: docs/30-messaging.md
"""
from core import CoreApp, AutoRouter
from src.apps.events.routes import router as events_router

# API router
api = AutoRouter(prefix="/api/v1")
api.include_router(events_router)

# App instance
app = CoreApp(routers=[api])
