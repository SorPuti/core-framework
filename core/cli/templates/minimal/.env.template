# Variáveis de ambiente. Docs: 02-settings.md. Troubleshooting: 99-faq-troubleshooting.md
# Edite e rode: core makemigrations --name initial && core migrate && core run

APP_NAME={{project_name}}
ENVIRONMENT=development
DEBUG=true
SECRET_KEY=change-me-in-production

# Database (SQLite já funciona; para PostgreSQL use a linha abaixo)
DATABASE_URL=sqlite+aiosqlite:///./app.db
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/dbname
