"""
App entry point. Docs: 01-quickstart.md, 23-routing.md
Auth e middleware vêm de src/settings.py (plug-and-play).
"""
from core import CoreApp, AutoRouter
from src.apps.users.routes import users_router, auth_router

api = AutoRouter(prefix="/api/v1")
api.include_router(users_router)
api.include_router(auth_router)

app = CoreApp(routers=[api])
